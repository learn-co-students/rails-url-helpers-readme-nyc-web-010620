<%# <% @posts.each do |post| %>
  <%# <div><%= link_to post.title, "/posts/#{post.id}" %></div>
<%# <% end %> 

<%# we can refactor the above: Instead of hard-coding the path and using string interpolation, let's use post_path and pass in the post argument. %>
<%# <% @posts.each do |post| %>
  <%# <div><%= link_to post.title, post_path(post.id) %></div>
<%# <% end %> 

<%# let's make one last refactor: Rails is smart enough to know that if you pass in the post object as an argument, it will automatically use the ID attribute, so we'll use this implementation code: %>
<% @posts.each do |post| %>
  <div><%= link_to post.title, post_path(post) %></div>
<% end %>